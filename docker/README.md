# Docker Containerization

Contains Dockerfiles and Compose configurations for running InsightRAG:
- `backend.Dockerfile`: FastAPI application with Python 3.12
- `frontend.Dockerfile`: Next.js production build
- `docker-compose.yml`: Multi-service orchestration including PostgreSQL + pgvector

